name: On Release Commit
on: 
  push:
    branches:
      - 'release/*'
jobs:
  call-on-release-commit:
    uses: .github/workflows/ReusableGetBranchesToPropagate.yaml@master
    with:
      branch: ${{github.ref_name}}
    secrets:
      token: ${{secrets.TOKEN}}