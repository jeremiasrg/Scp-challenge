name: On Release Push
on: 
  push:
    branches:
      - 'release/*'
jobs:
  call-on-release-commit:
    uses: ./.github/workflows/ReusableChangePropagate.yaml
    with:
      branch: ${{github.ref_name}}
    secrets:
      token: ${{secrets.TOKEN}}